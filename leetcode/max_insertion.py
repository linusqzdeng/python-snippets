# Find the maximum value of a given number n
# after insert x into one of the digits within n.

from itertools import permutations


def max_value(n: str, x: int) -> str:
    pass


a = '123'

b = a + '3'
print(list(permutations([i for i in b])))
